#include <stdio.h>

/**
 * main - Entry point starts here
 *
 * Return: Always zero (Success)
 */
int main(void)
{
	/*string is initialized*/
	char myStr[] = "\"Programming is like building a multilingual puzzle";

	puts(myStr);/*prints the string*/
	return (0);
}
